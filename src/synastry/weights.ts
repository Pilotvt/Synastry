export const WEIGHTS = {
  ascendant: 0.16,     // ASC×ASC (было 15%)
  moonToMoon: 0.24,    // Луна↔Луна (было 20%; -1% для перераспределения на нумерологию)
  sunToSun: 0.09,      // Солнце↔Солнце (было 7%)
  sunMoonCross: 0.17,  // Солнце↔Луна (было 12%; -1% для перераспределения на нумерологию)
  venusMars: 0.22,     // Венера↔Марс (было 18%)
  overlays: 0.00,      // оверлеи планета→дом (исключены из расчёта)
  nodes: 0.00,         // не используем в UI итоговой суммы
  numerology: 0.06,    // нумерология (ЧД/ЧС направленно) — +1%
  numerologyExpr: 0.06 // нумерология Экспрессия (по дате) — +1%
} as const;

export type Weights = typeof WEIGHTS;
